<?php include_once '../app/Views/Layout/mainHome.phtml'?>

    <section class="container mt-20">
        <h3><?php echo $args['objective'][0]['title']; ?></h3>
        <div class="btn-group">
            <a href="/key-result/newKeyResult/<?php echo $args['objective'][0]['id']?>" class="btn">Novo resultado chave</a>
        </div>
        <hr>

        <div class="cards">
            <?php if (empty($args['keyResults'])): ?>
                <p>Esse objetivo ainda não tem nenhum resultado chave cadastrado</p>
            <?php else: ?>
                <?php foreach ($args['keyResults'] as $keyResult): ?>
                    <div class="card-objective">

                        <h3><?php echo $keyResult['title'] ?></h3>
                        <p>Tipo: <?php echo $keyResult['type'] == 1 ? 'Milestone' : 'Porcentagem' ?></p>
                        <p><?php echo $keyResult['description'] ?></p>
                        <hr>
                        <div class="btn-group">
                            <a href="/key-result/edit/<?php echo $keyResult['id'] ?>" class="btn">Editar</a>
                            <?php if ($keyResult['deleted_at'] === null): ?>
                                <a href="/key-result/remove/<?php echo $keyResult['id'] ?>" class="btn-danger">Apagar</a>
                            <?php else: ?>
                                <a href="/key-result/restoreKeyResult/<?php echo $keyResult['id'] ?>" class="btn-danger">Restaurar</a>
                            <?php endif;?>
                        </div>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>
    </section>

<?php include_once '../app/Views/Layout/mainEnd.phtml'?>